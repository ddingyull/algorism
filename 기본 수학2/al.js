let sum = 0;
for(let i =0; i<101; i++){
  sum += i;
}

// for of로 쓴다면
let sum = 0;
for (let i of sum ){
  sum += i
}


let fruits1 = ["사과", "딸기", "파인애플", "수박", "참외", "오렌지", "자두", "망고"];
let fruits2 = ["수박", "사과", "참외", "오렌지", "파인애플", "망고"]
let arr;

same = fruits1.filter('"수박", "사과", "참외", "오렌지", "파인애플", "망고"')

console.log(same);
console.log(diff);


//짝수와 홀수
char* solution(int num) {
  let answer = '';
  if(num % ===0){
      return answer = "even"
  } else {
      return answer = "Odd"
  }

//평균구하기
function solution(arr){
  let sumArr = 0;
  for(let i = 0; i < arr.length; i++){
      sumArr +=arr[i];
      let average = summArr/ arr.length;
}
return average
}
//수박수박
char* solution(int n) {
  let answer = "";
  for(let i = 0; i<n; i++) {
      result += i%2===0 ? "수" : "박";
  }
  return answer;
}

//약수의 합
function solution(n) {
  var answer = 0;
  for (var i=1; i<=n;i++){
      if(n%i===0){answer+=i}
  }
  return answer;
}
//번호 가리기
//약수의 합
//모의고사

